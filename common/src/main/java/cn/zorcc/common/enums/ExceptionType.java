package cn.zorcc.common.enums;

/**
 * 定义FrameworkException类型
 */
public enum ExceptionType {
    NATIVE,
    WHEEL,
    CONTEXT,
    CONFIG,
    HTTP,
    JSON,
    SQLITE,
    DUCKDB,
    NETWORK,
    SQL,
    CLUSTER,
    AUTH,
    FILE,
    LOG,
    COMPRESS,
    MINT
}
