package cn.zorcc.common.enums;

/**
 * 定义FrameworkException类型
 */
public enum ExceptionType {
    NATIVE,
    WHEEL,
    CONTEXT,
    CONFIG,
    HTTP,
    JSON,
    SQLITE,
    NETWORK,
    JOB,
    SQL,
    CLUSTER,
    AUTH,
    FILE,
    LOG
}
