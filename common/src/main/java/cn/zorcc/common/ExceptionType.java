package cn.zorcc.common;

/**
 * 定义FrameworkException类型
 */
public enum ExceptionType {
    NATIVE,
    WHEEL,
    CONTEXT,
    CONFIG,
    HTTP,
    JSON,
    SQLITE,
    DUCKDB,
    NETWORK,
    SQL,
    POSTGRESQL,
    CLUSTER,
    AUTH,
    FILE,
    LOG,
    COMPRESS,
    MINT
}
